{
  "name": "kb-core",
  "version": "0.1.6",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup && tsc --declaration --emitDeclarationOnly --outDir dist --project tsconfig.json",
    "dev": "tsup --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "@iarna/toml": "^2.2.5",
    "@types/better-sqlite3": "^7.6.0",
    "better-sqlite3": "^9.0.0",
    "gray-matter": "^4.0.3",
    "remark": "^15.0.0",
    "remark-frontmatter": "^5.0.0",
    "remark-wiki-link": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.3.0",
    "vitest": "^2.1.0"
  }
}
